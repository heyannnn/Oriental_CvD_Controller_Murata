# Core dependencies for motor control
pymodbus>=3.0.0
pyserial>=3.5
numpy>=1.20.0

# Communication (OSC for inter-station sync)
python-osc>=1.8.0

# USB keyboard listener (master station only)
pynput>=1.7.0

# Raspberry Pi GPIO (for LED strips only - stations 7 & 10)
gpiozero>=1.6.0

# --------------------------------------------------------------------------
# Optional / Testing only:
# --------------------------------------------------------------------------
readchar>=4.0.0          # For test_station2_nogpio.py (keyboard input on Mac)
